# POC: use the public npm registry instead of the corporate Artifactory mirror.
# Scoped to this project only; the global ~/.npmrc registry/auth is left untouched.
registry=https://registry.npmjs.org/
