/**
 * Trivial scaffold-time helper used to prove the toolchain (TypeScript +
 * Vitest) is wired correctly. Safe to remove once real domain code exists.
 */
export function scaffoldHealthcheck(): string {
  return "ok";
}
